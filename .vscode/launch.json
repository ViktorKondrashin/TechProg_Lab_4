{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run/Debug app.exe (gdb)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\build\\app.exe",
      "cwd": "${workspaceFolder}",
      "args": [],
      "stopAtEntry": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "preLaunchTask": "build (g++)"
    }
  ]
}
